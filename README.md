# vitte_dev_for_deploy
business

#  Roadmap рефакторинга

> **Текущее состояние:** Монолитная архитектура, не масштабируется, падает при минимальной нагрузке нагрузке
> **Цель:** Микросервисная архитектура на Docker, выдерживает 5000+ / 10000 + активных пользователей

---

##  Текущие проблемы (простым языком)

### 🔴 Критические - требуют немедленного решения

#### 1. **Всё работает в одной программе**
**Что это значит:**
Представьте, что у вас один сотрудник делает всё: отвечает клиентам, обрабатывает платежи, генерирует картинки. Если он устанет или заболеет - весь бизнес встанет.

**Сейчас:**
- Бот, API, генерация картинок - всё в одном Python процессе
- Если упадёт генерация картинок → перестанет работать весь бот
- При 30+ одновременных пользователях процесс перегружается и падает

**Файл:** `backend/app/main.py` строки 27-29, 227-230

---

#### 2. **Искусственный интеллект блокирует работу**
**Что это значит:**
Когда пользователь пишет сообщение, бот отправляет запрос к DeepSeek AI. Пока AI думает (3-5 секунд), весь бот заморожен и не может отвечать другим пользователям.

**Аналогия:**
Как если бы кассир в магазине каждый раз уходил на склад за товаром, а очередь из 30 человек стояла и ждала.

**Сейчас:**
- 1 пользователь пишет → AI думает 3 сек → бот заморожен
- 10 пользователей одновременно → очередь 30 секунд
- При 50+ пользователях → Telegram отключает webhook (timeout)

**Файл:** `backend/app/services/chat_flow.py` строка 376-377

---

#### 3. **Генерация картинок парализует бот**
**Что это значит:**
Когда пользователь запрашивает картинку, генерация занимает 30-120 секунд. В это время бот не может отвечать другим пользователям.

**Сейчас:**
- 1 пользователь генерирует → 30-120 сек блокировки
- 2-й пользователь запрашивает картинку → ждёт, пока первый закончит
- Максимум 2 картинки одновременно (настройка в коде)

**Файл:** `backend/app/services/image_generation.py` строка 211-230

---

#### 4. **База данных работает нестабильно**
**Что это значит:**
При каждом запуске бота выполняются SQL команды для изменения структуры БД.

**Проблемы:**
- Невозможно откатить изменения при ошибке
- При масштабировании (сервер цпу) возникают конфликты
- При падении посередине операции - БД в сломанном состоянии

**Файл:** `backend/app/main.py` строки 69-217 (148 строк SQL!)

---

#### 5. **Нет кеширования - база данных перегружена**
**Что это значит:**
Каждое сообщение от пользователя = 5 запросов в базу данных. При 30-50 пользователях = 250 запросов в секунду.

**Аналогия:**
Как если бы библиотекарь каждый раз ходил в архив за одной и той же книгой, вместо того чтобы положить её на стол.

**Сейчас:**
```
Пользователь написал → БД запрос 1 (данные пользователя)
                      → БД запрос 2 (персонаж)
                      → БД запрос 3 (подписка)
                      → БД запрос 4 (настройки)
                      → БД запрос 5 (история диалога)
```

**Что нужно:**
Redis кеш - хранит часто используемые данные в памяти (в 1000 раз быстрее БД)

**Файл:** `backend/app/services/chat_flow.py` строки 83-93

---

#### 6. **Нет защиты от спама**
**Что это значит:**
Любой может отправить 1000 сообщений в секунду и положить сервер. Нет ограничений на частоту запросов.

**Сейчас:**
- Нет проверки "сколько сообщений пользователь отправил за минуту"
- Можно легко сделать DDoS атаку
- Злоумышленник может накрутить расходы на DeepSeek API ( либо другая модель с OpenRouter которая стоит дороже )

**Файл:** `backend/app/bot.py` строки 312-359

---

### ⚠️ Архитектурные - затрудняют разработку

#### 7. **Весь код в одном большом файле**
**Что это значит:**
Файл `bot.py` содержит 605 строк: онбординг, оплата, картинки, диалоги - всё вместе. Как книга без глав.

**Проблемы:**
- Сложно найти нужное место
- Изменение одной функции может сломать другую
- Невозможно разделить работу между разработчиками ( либо один разработчик будет разбираться очень долго, если оставим все как есть )

**Файл:** `backend/app/bot.py` (605 строк)

---

#### 8. **Функция, которая делает ВСЁ**
**Что это значит:**
Есть одна огромная функция `generate_chat_reply` на 185 строк, которая делает:
- Проверяет пользователя
- Загружает историю
- Проверяет безопасность
- Отправляет запрос к AI
- Генерирует голос
- Сохраняет в БД

**Аналогия:**
Как если бы один человек был кассиром, бухгалтером, грузчиком и уборщиком одновременно.

**Проблемы:**
- Нельзя заменить DeepSeek на GPT без переписывания всего
- Нельзя добавить повторную попытку только для AI
- Невозможно протестировать отдельные части

**Файл:** `backend/app/services/chat_flow.py` строки 251-436 (185 строк!)

**Коротко:**
chat_flow.py - божественный файл
От него нужно избавляться
---

#### 9. **База данных не выдерживает нагрузку**
**Что это значит:**
В настройках БД указано "максимум 5 одновременных подключений". При 10+ пользователях подключения кончаются.

**Сейчас:**
- pool_size = 5 (по умолчанию)
- При 10 одновременных пользователях → ошибка "QueuePool limit exceeded"

**Файл:** `backend/app/db.py` строки 11-15

---

## 🎯 Этапы рефакторинга

### **Этап 1: Docker + Разделение сервисов**

#### Что делаем:
1. Создаём `docker-compose.yml` с сервисами:
   - `bot` - aiogram (Telegram bot)
   - `api` - FastAPI (REST API)
   - `worker` - Celery (фоновые задачи)
   - `postgres` - PostgreSQL
   - `redis` - кеш + очереди

2. Переносим background workers в Celery
3. Добавляем Redis для кеширования

#### Почему сейчас плохо:
**Техническая проблема:** Monolithic architecture с единой точкой отказа (Single Point of Failure).

```
┌─────────────────────────────────┐
│    ОДИН Python процесс          │
│  Bot + API + Workers + LLM      │
└─────────────────────────────────┘
     ↓ Cascade failure
```

**Конкретные проблемы:**
- **Cascade failure:** Генерация картинки зависла → весь бот не работает
- **Zero downtime deployment невозможен:** Надо обновить код → перезапуск всего процесса (API и бот падают)
- **Horizontal scaling заблокирован:** Нельзя запустить несколько инстансов (конфликты в БД миграциях)
- **Resource contention:** LLM запрос забирает CPU → другие запросы тормозят

#### Почему будет хорошо:
**Техническое решение:** Microservices с изолированными процессами и fault isolation.

```
┌──────┐  ┌──────┐  ┌─────────┐
│ Bot  │  │ API  │  │ Workers │
└───┬──┘  └───┬──┘  └────┬────┘
    └─────────┴──────────┘
           Redis + PG
```

**Конкретные улучшения:**
- ✅ **Fault isolation:** Упал worker (картинки) → бот всё равно отвечает на сообщения
- ✅ **Independent scaling:** Можно запустить 10 ботов, 5 API, 20 воркеров - каждый масштабируется отдельно
- ✅ **Zero downtime deployments:** Обновление бота не затрагивает API (rolling updates)
- ✅ **Async task processing:** Очереди в Redis → картинки генерируются параллельно через worker pool

#### Стек:
- **Docker** + **Docker Compose**
- **Redis** (кеш + Celery broker)
- **Celery** (фоновые задачи)
- **Nginx** (reverse proxy, опционально)

---

### **Этап 2: Миграции БД + Connection Pool**

#### Что делаем:
1. Убираем весь SQL из `main.py:69-217`
2. Переносим в Alembic миграции
3. Настраиваем connection pool:
   ```python
   engine = create_async_engine(
       settings.database_url,
       pool_size=20,           # было: 5
       max_overflow=40,        # было: 10
       pool_pre_ping=True,
   )
   ```

#### Почему сейчас плохо:
**Техническая проблема:** Imperative schema migrations в application code + недостаточный connection pooling.

**Конкретные проблемы:**
- **Non-idempotent migrations:** При каждом запуске выполняется 148 строк SQL (ALTER TABLE) → race conditions
- **No rollback strategy:** Упал сервер посередине миграции → БД в inconsistent state
- **Deployment риски:** Откатить изменения невозможно - нет версионирования схемы
- **Connection pool exhaustion:** pool_size = 5 → при 10+ concurrent users выдаёт "QueuePool limit exceeded"
- **No health checks:** Нет `pool_pre_ping` → dead connections не отсеиваются

#### Почему будет хорошо:
**Техническое решение:** Declarative migrations (Alembic) + правильная настройка connection pool.

**Конкретные улучшения:**
- ✅ **Version control для схемы:** Миграции выполняются один раз, записывается версия в `alembic_version`
- ✅ **Rollback capability:** Можно откатить любое изменение командой `alembic downgrade -1`
- ✅ **Idempotent deploys:** При запуске 10 инстансов - конфликтов нет (миграция уже применена)
- ✅ **Proper pooling:** Connection pool увеличен с 5 до 20 + overflow 40 → выдерживает 100+ concurrent requests
- ✅ **Blue-green deployments:** Безопасный деплой с backward compatible миграциями

#### Стек:
- **Alembic** (миграции)
- **SQLAlchemy** (настройка pool)

---

### **Этап 3: Кеширование**

#### Что делаем:
1. Кешируем в Redis:
   - Активные персоны пользователя (TTL 5 мин)
   - Subscription status (TTL 1 час)
   - Feature unlocks (TTL 10 мин)

2. Добавляем декоратор `@cached`:
   ```python
   @cached(ttl=300, key="persona:{user_id}")
   async def get_active_persona(user_id: int):
       ...
   ```

#### Почему сейчас плохо:
**Техническая проблема:** No caching layer → каждый запрос идёт в PostgreSQL (high latency, DB bottleneck).

**Что происходит сейчас:**
```python
# Каждое сообщение = 5 SELECT запросов в БД
user = await get_user(...)          # SELECT users - 50ms
persona = await get_persona(...)    # SELECT personas - 50ms
subscription = await get_sub(...)   # SELECT subscriptions - 50ms
features = await get_features(...)  # SELECT feature_unlocks - 50ms
dialog = await get_dialog(...)      # SELECT dialogs - 100ms
                                    # ИТОГО: 300ms только на БД queries
```

**При нагрузке:**
- 50 concurrent users × 5 queries = **250 queries/sec** → PostgreSQL становится bottleneck
- PostgreSQL начинает throttling при 300+ queries/sec
- Latency растёт с 2 сек до 10-15 сек (queue wait time)
- Database становится single point of failure для performance

#### Почему будет хорошо:
**Техническое решение:** Redis in-memory cache с TTL стратегией (Cache-Aside pattern).

**Что будет:**
```python
# Cache-Aside pattern с lazy loading
user = await cache.get("user:{id}")     # ~1ms из Redis (in-memory)
if not user:
    user = await get_user_from_db(...)  # Cache miss → БД
    await cache.set("user:{id}", user, ttl=300)  # Write-through в кеш
```

**Результат:**
- 50 concurrent users × 1 query (cache miss rate ~5%) = **50 queries/sec** → 80% reduction
- ✅ **Latency improvement:** 300ms → 10ms для кешированных данных
- ✅ **DB offload:** БД разгружается на 80%, запас для масштабирования
- ✅ **Throughput increase:** Можно обслужить в 5 раз больше пользователей без upgrade БД

#### Стек:
- **Redis** (кеш)
- **redis-py** или **aioredis**

---

### **Этап 4: Разбиваем монолиты**

#### Что делаем:

**1. Разбиваем `bot.py` (605 строк → модули)**
```
bot/
├── handlers/
│   ├── start.py         # Онбординг, /start, /help
│   ├── chat.py          # Обработка сообщений
│   ├── payments.py      # /pay, Stars, YooKassa
│   └── images.py        # Генерация картинок
├── keyboards/
│   ├── main_menu.py
│   └── payments.py
├── middlewares/
│   ├── access.py        # Только проверка, БЕЗ изменений
│   └── terms_gate.py
└── main.py              # Entry point
```

**2. Разбиваем `chat_flow.py` (533 строки → сервисы)**
```python
# Было: 1 функция делает ВСЁ
async def generate_chat_reply(...):  # 185 строк

# Станет: отдельные сервисы
class PersonaService:
    async def get_active(user_id) -> Persona

class DialogService:
    async def get_or_create(user_id, persona_id) -> Dialog

class MemoryService:
    async def build_context(dialog) -> str

class SafetyService:
    def check(input_text) -> SafetyResult

class IntimacyService:
    def decide(context) -> IntimacyDecision

class LLMService:
    async def complete(messages) -> str
    async def complete_with_retry(messages) -> str  # + retry logic

class MessageService:
    async def save(dialog_id, role, content)
```

#### Почему сейчас плохо:
**Техническая проблема:** God Object anti-pattern — одна функция делает всё (185 строк, high coupling, low cohesion).

**Конкретные проблемы:**
- **Tight coupling:** Хотим добавить retry для LLM → надо рефакторить всю функцию на 185 строк
- **No unit testing:** Проверка безопасности вшита внутрь → невозможно изолированно протестировать
- **Vendor lock-in:** Хотим заменить DeepSeek на GPT-4 → LLM logic размазана по всей функции
- **High cognitive load:** Новому разработчику нужно неделя, чтобы разобраться (cyclomatic complexity ~25)

#### Почему будет хорошо:
**Техническое решение:** Service Layer pattern с Dependency Injection (SOLID principles).

**Конкретные улучшения:**
- ✅ **Single Responsibility:** Каждый сервис делает одно дело (high cohesion):
  - `LLMService` - только LLM communication с retry logic
  - `SafetyService` - только content moderation
  - `MemoryService` - только context management
- ✅ **Easy to extend:** Добавить retry: `LLMService.complete_with_retry()` - 10 строк кода
- ✅ **Pluggable architecture:** Заменить DeepSeek на GPT: меняем только `LLMService` implementation
- ✅ **Low cognitive load:** Новый разработчик разбирается за 1 день (каждый сервис 50-100 строк)
- ✅ **Unit testable:** Можно тестировать каждый сервис изолированно с mocks (TDD-friendly)

#### Стек:
- **Архитектурные паттерны:** Service Layer, Dependency Injection
- **Тесты:** pytest, pytest-asyncio

---

### **Этап 5: Очереди для тяжёлых задач**

#### Что делаем:
1. Переносим в Celery:
   - Генерация изображений (30-120 сек)
   - Retention задачи (напоминания)
   - Рассылки

2. Делаем async-sync flow:
   ```python
   # Bot (async)
   task = generate_image.delay(user_id, persona_id)
   await message.answer("Генерирую, жди 30 сек ⏳")

   # Worker (sync Celery task)
   @celery.task
   def generate_image(user_id, persona_id):
       image_url = comfyui_client.generate(...)
       bot.send_photo(user_id, image_url)
   ```

#### Почему сейчас плохо:
**Техническая проблема:** Blocking I/O в async event loop — long-running tasks блокируют весь процесс.

**Что происходит сейчас:**
```python
# Пользователь нажал кнопку "Посмотреть картинку"
await request_image_on_demand(...)  # 30-120 секунд blocking I/O!
# Event loop frozen → все остальные requests ждут
```

**Конкретные проблемы:**
- **Head-of-line blocking:** User 1 запросил картинку → event loop заморожен на 60 сек
- **Poor concurrency:** Максимум 2 картинки одновременно (`concurrency=2` в коде)
- **Timeout risk:** При 60+ сек генерации → Telegram webhook timeout (60 сек)
- **Cascading failure:** Если ComfyUI зависнет → весь бот падает (no circuit breaker)

#### Почему будет хорошо:
**Техническое решение:** Task Queue (Celery) с async-sync bridge и worker pool.

**Что будет:**
```python
# Bot (async) - мгновенный ответ
task = generate_image.delay(user_id, persona_id)  # Enqueue в Redis
await message.answer("Генерирую, жди 30-60 сек ⏳")

# Worker pool (sync Celery tasks) - параллельная обработка
@celery.task
def generate_image(...):
    image = comfyui.generate(...)  # 60 секунд, но в отдельном процессе
    bot.send_photo(user_id, image)
```

**Результат:**
- ✅ **Non-blocking:** Бот отвечает мгновенно, картинка придёт асинхронно через callback
- ✅ **Parallel processing:** 20 workers = 20 картинок параллельно (вместо 2)
- ✅ **Fault tolerance:** ComfyUI зависнул → бот работает, worker retry автоматически
- ✅ **Progress tracking:** Можно показать прогресс через task status: "Генерация 30% готова..."

#### Стек:
- **Celery** (task queue)
- **Redis** (broker + result backend)

---

### **Этап 6: Rate Limiting + Monitoring**

#### Что делаем:
1. Добавляем rate limiter:
   ```python
   @rate_limit("10/minute", "100/hour")
   async def on_user_message(message):
       ...
   ```

2. Добавляем мониторинг:
   - **Sentry** - ошибки
   - **Prometheus** + **Grafana** - метрики
   - **Structured logging** - JSON логи

#### Почему сейчас плохо:
**Техническая проблема:** No rate limiting + no observability → DDoS уязвимость и blind operations.

**Конкретные проблемы:**
- **DDoS vulnerable:** Злоумышленник может отправить 1000 req/sec → CPU exhaustion
- **API abuse:** Боты накручивают расходы на DeepSeek API (прецеденты были)
- **No alerting:** Бот падает, узнаём только когда пользователи жалуются (MTTR высокий)
- **Unstructured logs:** Логи `INFO: Message received` - impossible to grep, no trace ID
- **No metrics:** Нет visibility: сколько RPS, какая latency, где bottleneck

#### Почему будет хорошо:
**Техническое решение:** Rate limiting + observability stack (metrics, logs, traces).

**Конкретные улучшения:**
- ✅ **Rate Limiting (Token Bucket algorithm):** 10 req/min, 100 req/hour per user
  - Превышение → HTTP 429 "Too Many Requests"
  - Автоматическая блокировка bot-like patterns
- ✅ **Error tracking (Sentry):**
  - Exception → instant alert в Telegram с stack trace
  - Breadcrumbs: sequence of events leading to error
- ✅ **Metrics & Dashboards (Prometheus + Grafana):**
  - Real-time graphs: active users, RPS, latency percentiles (p50, p95, p99)
  - SLO monitoring: if error rate > 5% → PagerDuty alert
- ✅ **Structured logging (JSON):**
  - Было: `INFO: Message received`
  - Стало: `{"timestamp": "2025-01-07T12:00:00Z", "user_id": 123, "trace_id": "abc123", "latency_ms": 1200}`
  - Легко query: `grep trace_id=abc123` или через Elasticsearch

#### Стек:
- **slowapi** или **aiolimiter** (rate limiting)
- **Sentry** (error tracking)
- **Prometheus** + **Grafana** (метрики)

---

## 📦 Финальный стек технологий

### Backend
- **Python 3.11+**
- **FastAPI** - REST API
- **aiogram 3.x** - Telegram bot
- **SQLAlchemy 2.0** - ORM
- **Alembic** - миграции БД

### Инфраструктура
- **Docker** + **Docker Compose**
- **PostgreSQL 15+** - основная БД
- **Redis 7+** - кеш + очереди
- **Celery** - фоновые задачи
- **Nginx** - reverse proxy (опционально)

### Интеграции
- **OpenRouter / ProxyAPI** - DeepSeek LLM
- **ComfyUI** - генерация изображений
- **Telegram Stars** + **YooKassa** - платежи

### Мониторинг
- **Sentry** - error tracking
- **Prometheus** + **Grafana** - метрики
- **Structured logging** - JSON логи

---


## 🚀 План деплоя

### Поэтапный переход (без даунтайма)

**Week 1:** Этап 1 - Docker + разделение сервисов
**Week 2:** Этап 2 - Миграции БД + Этап 3 - Кеширование
**Week 3:** Этап 4 - Разбиваем монолиты
**Week 4:** Этап 5 - Очереди + Этап 6 - Monitoring

### Тестирование
- Unit тесты для каждого сервиса
- Integration тесты для API
- Load testing (500+ одновременных пользователей)
- Staging окружение = копия продакшена

---

## ✅ Checklist готовности к продакшену

- [ ] Docker Compose развёрнут
- [ ] Redis кеш работает
- [ ] Celery workers запущены
- [ ] Alembic миграции настроены
- [ ] Сервисы разбиты на модули
- [ ] Rate limiting настроен
- [ ] Sentry подключен
- [ ] Grafana дашборды созданы
- [ ] Load testing пройден (5000+ пользователей)
- [ ] CI/CD pipeline настроен
- [ ] Backup БД автоматизирован
- [ ] Мониторинг алертов в Telegram

Тут будем отмечать галочки
---

**Общий срок полного рефакторинга:** 3 недели

---

## 📝 Краткое резюме

### Главная проблема
Бот работает как **один человек**, который делает всё: принимает заказы, готовит, моет посуду, принимает оплату. При 50+ клиентах он физически не справляется и всё рушится.

### Решение
Разделить на **команду специалистов**: официант (бот), повар (worker), кассир (API). Каждый делает своё дело. Если повар заболел - хотя бы принимаем заказы.

### Что получим
1. **Выдержит нагрузку в 100 раз больше:** с 50 до 5000 пользователей
2. **В 4 раза быстрее:** с 2-5 сек до 0.5-1 сек
3. **Экономия 25%** на серверах (много маленьких дешевле одного большого)
4. **Защита от взлома:** rate limiting + мониторинг
5. **Быстрый откат:** ошибка в продакшене? Откатились за 30 секунд

### Сроки и приоритеты
- **Неделя 1 (критично):** Docker + разделение сервисов → бот не падает
- **Неделя 2 (важно):** БД миграции + кеширование → скорость ×4
- **Неделя 3-4 (улучшения):** Рефакторинг кода + мониторинг

### Риски текущей архитектуры
- ❌ При 50 пользователях бот гарантированно упадёт
- ❌ Нет защиты от DDoS → злоумышленник может накрутить $10,000 расходов на AI за ночь
- ❌ Упал бот → теряем пользователей, пока не заметим и не перезапустим вручную
- ❌ Нельзя масштабировать → застрянем на 50 пользователях навсегда
- ❌ Риск потерять базу данных / оплаты юзеров - 100%

### Почему нельзя откладывать
Если не сделать сейчас, при росте до 50+ пользователей:
1. Бот будет падать ежедневно
2. Придётся всё равно переписывать, но под давлением и с потерей пользователей
3. Переписывание под нагрузкой = в 3 раза дольше и дороже

**Вывод:** Лучше инвестировать недели сейчас, чем месяцы борьбы с падениями и потерей пользователей потом.
